language: python

python:
  - '3.7'

branches:
  only:
    - master

install:
  - pip install molecule docker==3.7.3 docker-compose

jobs:
  include:
    - stage: test
      script:
        - ansible --version
        - molecule --version
        - pip freeze
        - cd ansible/test--docker-compose--compose--create.role && molecule test
