version: '3'

services:
  server--draw-io:
    build:
      context:      './../../docker/server--draw-io'
      dockerfile:   'default.docker'
    container_name: 'server--draw-io'
    image:          'talsen-team/docker-draw-io:v10.1.7'
    ports:
      -             '80:80'
    restart:        'no'
